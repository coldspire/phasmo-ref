---
layout: ghostLayout

pagination:
    data: ghosts
    size: 1
    alias: ghost
permalink: "ghosts/{{ ghost.name | slugify }}.html"

title: Ghost
---
<h3>{{ ghost.name }}</h3>
<p>Hunts at sanity threshold: {{ ghost.huntSanityThreshold }}</p>
<h4>Evidence</h4>
<ul>
    {%- for evidence in ghost.evidence %}
        <li>{{ evidence }}</li>
    {% endfor -%}
</ul>
<h4>Unique</h4>
<ul>
    {%- for unique in ghost.uniqueInfo %}
        <li>{{ unique }}</li>
    {% endfor -%}
</ul>